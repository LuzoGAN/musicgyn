# Copiar para deploy/ como ".env" e TROCAR o segredo (nunca commitar o real).
# Gerar um: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=TROQUE-POR-64-HEX-ALEATORIOS
SESSION_COOKIE_NAME=gymbeats_session
CREATE_DEMO=1
# YOUTUBE_API_KEY=
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
